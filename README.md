# memory-poollib

Several lightweight, specialised implementations of memory pools in C++

## Types

- Bump Allocator (`include/bump_pool.hpp`)

- Arena Allocator (`include/arena_pool.hpp` - orchestrates `bump_pool`)
